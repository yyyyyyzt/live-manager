# 观众评论先审后发（已移除）

本仓库曾包含独立的 `@live/audit-server` 与 `/api/moderation/*` 代理。**当前版本已删除该能力与相关前端页面**。

若需「观众投稿不落公开弹幕、仅审核后展示」，需在业务侧自行实现：自建队列与服务端投递（例如审核通过后调用 Live Engine `send_text_msg` / IM 群消息），并在观众端关闭直达展示或改为轮询已通过列表。
